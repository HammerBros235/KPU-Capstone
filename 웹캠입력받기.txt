import cv2
 
cam = cv2.VideoCapture(0) #0이 되지않으면 1 내장,외장카메라 차이
cam.set(3,1280) #3, 4는 가로세로 길이
cam.set(4,720) 
 
while True:
    ret_val, img = cam.read() # 캠 이미지 불러오기
 
    cv2.imshow("Cam",img) # 캠 이미지 출력
    if cv2.waitKey(1) == 27:
        break  # esc to quit